/**
 * Full-viewport film-grain overlay. Pure CSS/SVG (see globals.css), animated by
 * stepping the background position. Adds analog texture over the flat dark UI.
 */
export default function Grain() {
  return <div className="grain" aria-hidden="true" />;
}
